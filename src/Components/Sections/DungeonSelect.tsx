import styles from '../../Styles/DungeonSelect.module.css'
import { InstanceProps } from '../../app/Type'

export default function DungeonSelect(props: InstanceProps) {
	console.log(props.instanceData)
	return (
		<div className={styles.container}>

		</div>
	)
}